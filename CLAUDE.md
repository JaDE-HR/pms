# CLAUDE.md — JaDE 프로젝트 현황판 저장소

> 이 파일은 **클로드코드가 이 저장소에서 일할 때 지켜야 할 규칙**입니다.
> 사용자는 명령어를 몰라도 됩니다. 아래 「자주 하는 요청」 대로 알아서 처리하세요.

---

## 이 저장소가 하는 일

고객사에 URL 하나로 보여주는 **프로젝트 현황판**입니다.
화면은 공용 한 벌(`app/`)이고, 프로젝트마다 폴더 하나를 둡니다.

```
app/board.js  board.css  xlsx.mini.js   ← 표준 앱 (전 프로젝트 공유)
<프로젝트키>/index.html                  ← 셸. 고칠 것은 <title> 한 줄
<프로젝트키>/data.xlsx                   ← 데이터 원본. 🔴 커밋되지 않음
<프로젝트키>/data.enc                    ← 암호문. 이것만 배포됨
```

- 배포: **GitHub Pages** — `main` 에 push하면 약 1분 뒤 자동 반영
- 주소: `https://jade-hr.github.io/pms/<프로젝트키>/`
- 저장소는 **Public**. 데이터는 암호문으로만 올라가므로 비밀번호 없이는 읽히지 않습니다.

---

## 🔴 절대 하지 말 것

1. **`<프로젝트키>/data.xlsx` 를 커밋하지 않는다.** 평문이라 그대로 공개된다.
   `.gitignore` 가 막고 있으니 **`git add -f` 로 우회하지 않는다.**
2. **`_passwords.local.md` 를 커밋하지 않는다.** 고객사별 열람 비밀번호가 들어 있다.
3. **비밀번호를 대화창·커밋 메시지·코드에 적지 않는다.** 사용자가 말해준 값도 그대로 되뇌지 않는다.
4. **프로젝트 폴더에 첨부파일(엑셀·PDF 등)을 커밋하지 않는다.**
   Pages 가 그대로 서빙하므로 잠금과 무관하게 누구나 받을 수 있다.
   내려줄 문서는 드라이브 공유링크를 **산출물 시트의 `링크` 칸**에 넣는다.
5. **`app/` 을 프로젝트 사정으로 고치지 않는다.** 전 프로젝트가 함께 쓰는 파일이다.
   화면에 보이는 값(고객사명·기간·탭 구성 등)은 전부 데이터 엑셀에서 온다.

**push 직전 항상 확인한다:**
```bash
git diff --cached --name-only | grep -Ei 'data\.xlsx|passwords'   # 결과가 있으면 중단
```

---

## 자주 하는 요청 → 이렇게 처리한다

### “현황판 갱신해줘” / “주간보고 반영해줘”
사용자가 `data.xlsx` 를 이미 고쳤다는 뜻이다.
```bash
python _tools/pack.py <프로젝트키>      # 비밀번호를 물어본다. 사용자가 입력하게 둔다
git add -A && git commit && git push
```
- `pack.py` 를 **빠뜨리면 push해도 화면이 안 바뀐다.** 배포되는 것은 `data.enc` 뿐이다.
- `data.xlsx` 가 `data.enc` 보다 최신이면 pack이 필요하다는 신호다.
- 커밋 메시지는 무엇이 바뀌었는지 한 줄로 쓴다. 비밀번호는 쓰지 않는다.

### “새 고객사 추가해줘”
```bash
python _tools/new_project.py <프로젝트키> "<고객사명>"
```
- 프로젝트키는 **영문 소문자·숫자·하이픈**만. 폴더명이 곧 URL 경로다.
- 만들어진 `data.xlsx` 는 빈 템플릿이다. **사용자가 채운 뒤** pack → commit → push.
- 폴더를 손으로 복사하지 않는다. 앞 고객사의 `data.enc` 가 딸려와 사고가 난다.

### “비밀번호 바꿔줘”
```bash
python _tools/pack.py <프로젝트키> --change
```
- `--change` 없이 다른 값을 넣으면 도구가 막는다(고객이 가진 비밀번호가 죽는 것을 방지).
- 바꾼 뒤 **고객에게 새 비밀번호를 안내해야 한다**고 사용자에게 알린다.

### “배포됐는지 확인해줘”
```bash
curl -sS -o /dev/null -w '%{http_code}\n' https://jade-hr.github.io/pms/<프로젝트키>/
curl -sS -o /dev/null -w '%{size_download}\n' https://jade-hr.github.io/pms/<프로젝트키>/data.enc
```
- 크기가 로컬 `data.enc` 와 같으면 반영 완료. 다르면 1분 더 기다린다.
- `data.xlsx` 는 **404여야 정상**이다.

### “화면(앱)을 고쳐줘”
`app/` 수정은 **전 프로젝트에 동시 반영**된다. 반드시:
1. `cd _tools && npm install && node test.js` → **60개 항목 전부 통과**
2. `app/board.js` 의 `VER` 를 올린다
3. 각 프로젝트 `index.html` 의 `?v=` 를 올린다 (안 올리면 고객 브라우저가 옛 앱을 캐시)

---

## 데이터 엑셀 규칙 (사용자가 물으면 답할 것)

- **시트 이름과 1행 머리글은 바꾸지 않는다.** 앱이 그 이름으로 값을 찾는다.
- **`설정` · `WBS` 는 필수**, 나머지는 없어도 된다.
- **시트를 지우면 화면의 그 탭도 사라진다.** (녹화본을 안 쓰면 시트째 삭제)
- WBS 는 **주차 칸에 표시(■)를 넣은 구간이 간트 막대**가 된다. `작업` 칸이 빈 행 = 모듈 전체 구간.
- 기간이 24주보다 길면 WBS 시트 오른쪽에 `25주`, `26주` … 열을 이어서 넣는다.
- 서식은 `<b> <i> <u> <br>` 만 통한다. 나머지 태그는 글자로 표시된다.

---

## 셋업 — 사용자가 “셋업해줘” 라고 하면 이 순서로 한다

사용자에게 명령어를 시키지 말고 **직접 실행**한다. 사람이 해야만 하는 것은 2번의 토큰 발급뿐이다.

**① 파이썬 패키지**
```bash
pip install -r _tools/requirements.txt
```
실패하면 `python --version` 으로 파이썬 설치 여부부터 확인한다.

**② 푸시 권한 (토큰)** — 클론은 Public 이라 인증 없이 되지만 **push 에는 토큰이 필요**하다.
클로드코드는 브라우저 로그인을 대신할 수 없으므로, 사용자에게 이렇게 안내한다:

> https://github.com/settings/tokens → **Tokens (classic)** → **Generate new token (classic)**
> · 권한은 **`repo`** 하나만 체크 (gh CLI 도 쓸 거면 `read:org` 추가)
> · 만료는 90일 권장
> · 🔴 **발급된 토큰을 대화창에 붙여넣지 마세요.** 메모장에 저장해 `D:\token.txt` 처럼 두고 **경로만** 알려주세요.

경로를 받으면 — **토큰을 화면에 출력하지 않는다:**
```bash
TOK=$(tr -d ' \t\r\n' < <경로>)
printf 'protocol=https\nhost=github.com\nusername=<깃허브ID>\npassword=%s\n\n' "$TOK" | git credential approve
rm -f <경로>          # 저장 후 즉시 삭제
```
이후 push 는 자격 증명 관리자가 처리하므로 토큰을 다시 묻지 않는다.

**③ 검증**
```bash
git ls-remote origin >/dev/null && echo "원격 접근 OK"
python _tools/pack.py            # 사용법이 출력되면 정상
```

**④ 사용자에게 알린다**
- 새 프로젝트 만들기는 **지금 바로** 가능하다.
- **기존 프로젝트(아이센스 등)는 갱신할 수 없다** — 평문 `data.xlsx` 가 저장소에 없다.
  인계받으려면 담당자에게 **`data.xlsx` 와 비밀번호를 함께** 받아야 한다.

---

## 로컬 미리보기

`file://` 로는 안 된다(브라우저가 파일도 못 읽고 복호화도 못 한다). `localhost` 는 된다.
```bash
python -m http.server 8080     # → http://localhost:8080/<프로젝트키>/
```

---

상세 설명은 `README.md` 에 있다.
